dist: trusty
language: java
jdk:
  - oraclejdk8
before_script:
  - mvn dependency:resolve-plugins dependency:resolve
script:
  - mvn clean install -Dgpg.skip=true -DskipTests
cache:
  directories:
  - $HOME/.m2
